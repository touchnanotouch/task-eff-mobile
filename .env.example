# Server Configuration

SERVER_HOST=0.0.0.0
SERVER_PORT=8080

SERVER_TIMEOUT=30

# Database Configuration

DB_HOST=localhost
DB_PORT=5432

DB_USER=postgres
DB_PASSWORD=postgres

DB_NAME=subscriptions
DB_SSLMODE=disable
